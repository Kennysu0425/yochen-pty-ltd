# Claude Code internal state — never commit
.claude/

# macOS noise
.DS_Store
**/.DS_Store
.localized

# Secrets / env
.env
.env.*
!.env.example

# Temp
*.swp
*.bak
*~

# Next.js / Node
node_modules/
.next/
out/
build/
dist/
next-env.d.ts
*.tsbuildinfo

# Vercel
.vercel

# Nested product repos — each has its own .git + remote, managed independently.
# Outer Yochen repo only tracks the landing page + admin notes.
products/

